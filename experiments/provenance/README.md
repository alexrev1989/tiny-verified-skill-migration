# Provenance

`MANIFEST_SHA256.json` is the unchanged manifest embedded in `Tiny_Verified_Skill_Migration_FINAL_Alexander_Revyakin.zip`. Its paths refer to that archive's original directory layout. All 23 listed files were verified against their byte counts and SHA-256 digests before this repository was assembled.
